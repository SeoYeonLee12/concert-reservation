# k6.json 을 그라파나에 자동으로 등록하는 설정

apiVersion: 1

providers:
  - name: "k6 Stress Test"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /dashboard/k6.json
      foldersFromFilesStructure: true