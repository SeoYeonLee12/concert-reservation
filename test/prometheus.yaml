# prometheus.yaml
# k6와 스프링부의 지표를 긁어오는 설정 파일

global:
  scrape_interval: 5s
scrape_configs:
  - job_name: 'spring-boot'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'host.docker.internal:8080' ]